# 实现位逆序置换的加法器（二进制加法器从左到右加一运算，对应算法导论17章思考题的第一题


void addrev() {
    
    int b = 1 << k; //b = 1 0 0 0 ... 0 0 (k个零) 
    while (n | b == n) {
      n &= ~b;
      b >>= 1;
     }
     n = n | b;
     return n;

  }
